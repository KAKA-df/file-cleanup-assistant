**# File Cleanup Assistant**

A small Python tool that scans folders and identifies files that may need cleanup

Current features:
- Recursively scans folders
- Detect large and old files
- Display human-readable file sizes
- Show a cleanup summary

This project is still under development
